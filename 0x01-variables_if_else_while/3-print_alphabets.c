#include <stdio.h>

/**
 * main - Entry point
 * 
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;
	int a;

	for (i = 'a'; i <= 'z'; i++)
	{
	putchar(i);
	}
         
       for ( a  = 'A'; a <= 'z'; i++)
       {
       putchar(a);
       }  
    
       putchar('\n');
       return (0);
}       
